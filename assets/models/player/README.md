# Player models

Production player file path:

`meadow_maiden_walk.glb`

Use the optimized Meshy build, not the original ~55 MB file. The optimized build keeps the rig and Casual Walk animation while reducing the embedded 8192×8192 texture to 1024×1024 for mobile web.

Do not Base64-embed production models in HTML or JavaScript.
