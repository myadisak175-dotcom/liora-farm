// Deprecated compatibility shim retained only for branches that imported the temporary V1 helper.
// Production runtime now uses CONFIG.distantRange from src/config.js, matching the supplied ZIP.
export { CONFIG as __unused } from "../../config.js";
